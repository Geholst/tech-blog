<section id="post-section" data-id="{{id}}">
  <h3 >{{title}}</h3>
  <form>
   
    <label for="post-name">Post</label>
    <h1>{{Users.id}}</h1>
    <textarea name="post-title"  id="title">{{title}}</textarea>
     <label for="content-name">Comment</label>
    <textarea name="content"  id="content">{{content}}</textarea>
   
      </div>
    </div>
    <button class="bg-blue-500 hover:bg-blue-700 text-white font-bold py-1 px-2 border border-blue-700 rounded w-30">Update Post</button>
  </form>
</section>
<script src="/public/editPost.js"></script>